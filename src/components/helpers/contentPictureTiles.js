import * as React from 'react';
import { Image, TouchableOpacity, Text } from 'react-native';

const ContentPictureTiles = (props) => {
  return (
    <Text>pictures</Text>
  );
}

export default ContentPictureTiles;