import * as React from 'react';
import { Image, TouchableOpacity, Text } from 'react-native';

const ContentVideoTiles = (props) => {
  return (
    <Text>videos</Text>
  );
}

export default ContentVideoTiles;